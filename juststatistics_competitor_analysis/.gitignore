# Secrets — never commit
.env
.mcp.json
.claude/

# n8n workflow backup exports (managed by backup_workflows.sh)
n8n/backup/*.json

# SSL certificates — mounted into nginx container, never stored in repo
nginx/ssl/

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db
